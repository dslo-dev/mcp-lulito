[project]
name = "claw"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ClaudioCasti", email = "sdeveloper.lm@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp<2",
    "python-dotenv>=1.2.2",
]

[project.scripts]
claw = "claw:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
